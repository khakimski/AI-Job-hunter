services:
  backend:
    build: ./backend
    container_name: jobpilot-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend:/app
    restart: unless-stopped
